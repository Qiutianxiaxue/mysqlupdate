# 主数据库配置（用于存储TableSchema等管理表）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mysql_update
DB_USER=root
DB_PASSWORD=your_password

# 基准数据库配置（用于表结构检测的参考数据库）
BASE_DB_HOST=localhost
BASE_DB_PORT=3306
BASE_DB_NAME=base_schema_database
BASE_DB_USER=root
BASE_DB_PASSWORD=your_password

# 应用配置
PORT=3000
NODE_ENV=development

# 日志配置
LOG_LEVEL=info 